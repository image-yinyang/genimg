{
  "name": "yinyang-genimg",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "remote": "wrangler dev --remote",
    "stylefix": "prettier -w src/*.js",
    "lint": "prettier -w src/*.js",
    "deploytail": "wrangler deploy && wrangler tail",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "wrangler": "^4.46.0"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.39",
    "base64-js": "^1.5.1",
    "mustache": "^4.2.0"
  }
}
